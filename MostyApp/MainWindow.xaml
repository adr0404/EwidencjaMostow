<Window x:Class="MostyApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MostyApp"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" Background="#2C3E50">
            <TextBlock Text="MENU MOSTÓW" Foreground="White" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,20,0,20"/>

            <Button Content="Lista Obiektów" Name="BtnLista" Click="BtnLista_Click" Height="45" Margin="10,5" Background="#34495E" Foreground="White" FontSize="14" Cursor="Hand"/>

            <Button Content="Dodaj Obiekt" Name="BtnDodaj" Click="BtnDodaj_Click" Height="45" Margin="10,5" Background="#34495E" Foreground="White" FontSize="14" Cursor="Hand"/>

            <Button Content="Inspektorzy" Name="BtnInspektorzy" Click="BtnInspektorzy_Click" Height="45" Margin="10,5" Background="#34495E" Foreground="White" FontSize="14" Cursor="Hand"/>
            <Button Content="Rejestr Przeglądów" Name="BtnPrzeglady" Click="BtnPrzeglady_Click" Height="45" Margin="10,5" Background="#34495E" Foreground="White" FontSize="14" FontWeight="Bold" Cursor="Hand"/>
        </StackPanel>

        <Frame Grid.Column="1" Name="MainFrame" NavigationUIVisibility="Hidden"/>
    </Grid>
</Window>
